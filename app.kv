BoxLayout:
    orientation: 'horizontal'
    BoxLayout:
        orientation: 'vertical'
        size_hint_x: 0.25
        Label:
            text: 'Sort by:'
        Spinner:
            id: sort_by
            text: "Alphabetical"
            values: ("Alphabetical", "Completion Status")
            on_text: app.sort_items(self.text)
        Label:
            text: 'Add New Movie...'
        Label:

            text: 'Title:'
        TextInput:
            id: added_title
            font_size: 10
            multiline: False
        Label:
            text: 'Category:'
        TextInput:
            id: added_category
            font_size: 10
            multiline: False
        Label:
            text: 'Year:'
        TextInput:
            id: added_year
            font_size: 10
            multiline: False
        Button:
            text: 'Add Movie'
            on_release: app.press_add_movie()
        Button:
            text: 'Clear'
            on_release: app.clear_status_text()
    BoxLayout:
        orientation: 'vertical'
        Label:
            text: app.watched_status_text
            size_hint_y:0.1
        BoxLayout:
            # this layout is populated with (button) widgets in app code
            id: entries_box
            orientation: 'vertical'
        BoxLayout:
            size_hint_y: 0.1
            orientation: 'vertical'
            Label:
                text: app.status_text
